# CLAUDE.md

Always read `PROJECT.md` and `docs/SRS.md` first — they are the source of truth for product shape and business rules. All shared agent rules live in `AGENTS.md`.

Before starting a task, also skim these two quick-lookup maps:
- `docs/DOMAIN_INDEX.md` — bản đồ file theo domain nghiệp vụ (tra nhanh vị trí code cần sửa).
- `docs/DECISIONS.md` — quyết định kiến trúc đã chốt và các đánh đổi (để không phá vỡ/hỏi lại).

@AGENTS.md
