# Server
PORT=4000
NODE_ENV=development
# Optional: base URL shown in OpenAPI spec (Swagger). Default http://localhost:4000
# PUBLIC_API_BASE_URL=http://localhost:4000

# MongoDB
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# Amazon Cognito (outputs from `cdk deploy`)
COGNITO_USER_POOL_ID=replace_with_cognito_user_pool_id
COGNITO_CLIENT_ID=replace_with_cognito_client_id
COGNITO_REGION=ap-southeast-1

# AWS
AWS_REGION=ap-southeast-1
AWS_ACCESS_KEY_ID=replace_with_aws_access_key_id
AWS_SECRET_ACCESS_KEY=replace_with_aws_secret_access_key

# S3
S3_BUCKET_NAME=replace_with_s3_bucket_name

# Upload limits
MAX_UPLOAD_BYTES=52428800
S3_PRESIGN_EXPIRES_SECONDS=900

# ─────────────────────────────────────────────────────────────────────────────
# AI Provider configuration
# ─────────────────────────────────────────────────────────────────────────────
# Options:
#   local   → Embedding FREE in-process (@xenova/transformers, 384 dims). Chat needs cloud LLM.
#   gemini  → Embedding + chat via Vertex AI Gemini (ADC — service account, no API keys)
#   bedrock → Embedding + chat via AWS Bedrock (rollback)
#   auto    → Bedrock first, Vertex Gemini fallback (requires GOOGLE_CLOUD_PROJECT)
#
# Vertex AI setup: docs/post_deploy_setup.md section 5b
AI_PROVIDER=gemini

# Vertex AI — Application Default Credentials (org policy may disallow API keys)
GOOGLE_CLOUD_PROJECT=replace_with_gcp_project_id
GOOGLE_CLOUD_LOCATION=asia-southeast1
# Path to service account JSON (local dev). Production: inject via secret / mount.
GOOGLE_APPLICATION_CREDENTIALS=D:\path\to\vertex-sa-key.json

GEMINI_CHAT_MODEL=gemini-2.5-flash
GEMINI_EMBEDDING_MODEL=gemini-embedding-001
GEMINI_EMBEDDING_OUTPUT_DIMENSION=1024
GEMINI_EMBED_DELAY_MS=200
GEMINI_MAX_RETRIES=3
GEMINI_RETRY_BASE_MS=1000

# Amazon Bedrock — only when AI_PROVIDER=bedrock or auto
# BEDROCK_MODEL_ID=apac.amazon.nova-micro-v1:0
# BEDROCK_EMBEDDING_MODEL_ID=global.cohere.embed-v4:0
# BEDROCK_EMBEDDING_OUTPUT_DIMENSION=1024
# BEDROCK_EMBED_DELAY_MS=200
# BEDROCK_MAX_RETRIES=3
# BEDROCK_RETRY_BASE_MS=1000

CHAT_DAILY_LIMIT_PER_USER=50
