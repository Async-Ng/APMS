# Deprecated - Bedrock Vector Index Notes

APMS không còn dùng Bedrock/Claude/Titan cho stack AI hiện tại. Backend hiện dùng Vertex AI Gemini và `gemini-embedding-001`.

Tài liệu này được giữ lại như ghi chú lịch sử để tránh nhầm lẫn khi gặp branch hoặc dữ liệu cũ. Không dùng file này cho môi trường hiện tại.

Use instead:

```text
api/scripts/rebuild-vector-index.md
```
